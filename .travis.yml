language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"

virtualenv:
    system_site_packages: true

addons:
    apt:
        packages:
            - python-numpy 
            - python-scipy 
            - libblas-dev 
            - liblapack-dev
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests
